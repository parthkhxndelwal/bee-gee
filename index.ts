// Server-side stub. Zero imports on purpose: the background work happens in
// tui.tsx (resolved by the host at runtime), so this file must load under the
// server's resolver without needing node_modules.
export default {
  id: "bee-gee",
  setup() {},
}
